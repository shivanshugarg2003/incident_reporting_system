services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: incident-app
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      FLASK_ENV: production
      DATA_DIR: /app/data
    restart: unless-stopped
